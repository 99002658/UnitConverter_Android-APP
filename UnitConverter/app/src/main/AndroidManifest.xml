<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lnt.unitconverter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/preview"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CURRENCY_conv"></activity>
        <activity android:name=".WEIGHT_conv" />
        <activity android:name=".LENGTH_conv" />
        <activity android:name=".TEMPERATURE_conv" />
        <activity android:name=".AREA_conv" />
        <activity android:name=".PERCENT_calc" />
        <activity android:name=".BMI_calc" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>